services:

  # This is example nginx
  nginx-example:
    image: nginx:alpine
    container_name: nginx-example

  # This is example nginx
  mongodb-example:
    image: mongo:latest
    container_name: mongodb-example